```js
const configs = {
  "description": "Custom configurations for tmux, vim, and zsh.",
  "setup.sh": "Automated setup script to quickly configure your environment.",
  "usage": "Run `./setup.sh` to apply all configurations."
}
```
